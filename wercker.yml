box: hseeberger/scala-sbt
build:
  steps:
    - script:
        name: compile
        code: |
          sbt clean compile test
          
deploy:
  steps:
    - script:
        name: deploy
        code: |
          sbt clean compile publish

