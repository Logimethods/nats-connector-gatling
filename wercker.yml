box: hseeberger/scala-sbt
build:
  steps:
    - script:
        name: compile
        code: |
          sbt compile test
          
deploy:
  steps:
    - script:
        name: deploy
        code: |
          sbt deploy

